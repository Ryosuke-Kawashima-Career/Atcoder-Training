use proconio::{input, marker::Chars};

fn main() {
    input! {t: usize}
    for _case in 0..t {
        input! {n: usize, s: Chars, x: [i64; n], y: [i64; n-1]}
        let mut happiness: i64 = 0;
    }
}
